services:
  tandem:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      APP_URL: ${APP_URL:-http://localhost:3000}
      DATA_DIR: /data
      WEB_DIST: /app/web/dist
    volumes:
      - ./data:/data
